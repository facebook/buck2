load(":defs.bzl", "copy", "download")

copy(name = "copy", src = "src", hidden = "hidden")

download(
    name = "download",
    url = read_config("test", "url"),
    sha1 = read_config("test", "sha1"),
)
