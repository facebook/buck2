[buildfile]
    name = TARGETS.fixture

[repositories]
    root = .
    prelude = .

[buck2_resource_control]
    status = required
    enable_action_freezing = true
    memory_high_action_cgroup_pool = 200000000

[build]
    threads = 5
