load(":defs.bzl", "simple_rule")

simple_rule(
    name = "dummy",
    uses_experimental_content_based_path_hashing = read_config("test", "uses_experimental_content_based_path_hashing", "False") in ["True", "true"],
)

constraint_setting(name = "os")

constraint_value(
    name = "linux",
    constraint_setting = ":os",
)

platform(
    name = "linux_platform",
    constraint_values = [":linux"],
)
