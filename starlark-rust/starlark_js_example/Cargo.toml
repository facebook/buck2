[package]
description = "Example of running starlark-rust interpreter in browser"
edition = "2024"
name = "starlark_js_example"
publish = false
version = "0.0.0"

[dependencies]
starlark = { path = "../starlark", version = "0.14.2" }

[lib]
crate-type = ["cdylib", "rlib"]
