platform(name = "p")

stub(
    name = "target",
    exec_compatible_with = [
        "//config:minix",
    ],
    default_target_platform = ":p",
)

stub(
    name = "no_compatible_platform",
    exec_compatible_with = [
        "//config:plan9",
    ],
    default_target_platform = ":p",
)
