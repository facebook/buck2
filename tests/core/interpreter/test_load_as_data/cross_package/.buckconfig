[buildfile]
name=TARGETS.fixture

[buck2]
# Allow everything and use a single cell, so the only thing that can block the
# load below is the cross-package rule.
load_as_allowlist = *

[repositories]
root = .

[repository_aliases]
prelude = root
