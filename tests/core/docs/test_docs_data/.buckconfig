[buildfile]
name=TARGETS.fixture

[project]
ignore=ignored

[repositories]
root = .
cell = cell
prelude = prelude
fbcode = fbcode
fbsource = fbsource
buck = buck
config = config
toolchains = toolchains
special = special
prelude = prelude
