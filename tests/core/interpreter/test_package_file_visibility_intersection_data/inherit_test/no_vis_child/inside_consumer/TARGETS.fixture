# Consumer inside `root//inherit_test/...` (the parent's cap). Should
# succeed: the no-vis child must NOT have replaced the cap with empty.
stub(
    name = "c",
    deps = ["root//inherit_test/no_vis_child/target:t"],
)
