[package]
name = "small_map"
version = "0.1.0"
edition = "2021"
description = """
Fake SmallMap crate

It reexports starlark maps now, but will be a proper crate in the future
"""

[dependencies]
starlark_map = { version = "0.9.0-pre", path = "../../starlark-rust/starlark_map" }
