[buildfile]
name=TARGETS.fixture

[repositories]
root = .
fbcode = fbcode
fbsource = fbsource
buck = buck
toolchains = toolchains
prelude = prelude
config = config

[build]
execution_platforms = root//:platforms
