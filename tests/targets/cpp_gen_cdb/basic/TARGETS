load("@fbcode_macros//build_defs:cpp_library.bzl", "cpp_library")

cpp_library(
    name = "library",
    srcs = ["src/main.cpp"],
    preprocessor_flags = ["-DM_FOO_BAR=1"],  # an argument that would be shell quoted in an @argsfile
)
