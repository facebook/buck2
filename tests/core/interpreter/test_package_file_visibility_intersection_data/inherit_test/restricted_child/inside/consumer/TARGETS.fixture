# Consumer INSIDE the child's explicit `visibility=[...]` cap term.
# Both visibility (matches parent's broad list) AND cap
# (matches the narrowed list) check pass. The dep is allowed.
stub(
    name = "c",
    deps = ["root//inherit_test/restricted_child/target:t"],
)
