[buildfile]
name=TARGETS.fixture

[repositories]
root = .
prelude = prelude

[cell_aliases]
fbsource = root
fbcode = root
buck = root
config = prelude
ovr_config = prelude
toolchains = prelude

[external_cells]
prelude = bundled
