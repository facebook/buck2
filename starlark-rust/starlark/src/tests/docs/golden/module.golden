# @generated
# To regenerate, run:
# ```
# STARLARK_RUST_REGENERATE_DOC_TESTS=1 cargo test -p starlark --lib tests
# ```

# name

These are where the module docs go

## MAGIC

```python
MAGIC: int.type
```

---

## func1

```python
def func1(foo: str.type) -> str.type
```

Docs for func1

#### Parameters

* `foo`: Docs for foo


#### Returns

The string 'func1'

---

## func2

```python
def func2() -> str.type
```

---

## func3

```python
def func3(a1: int.type, a2: int.type = None, step: int.type = None) -> str.type
```

A function with only positional arguments.

And a slightly longer description. With some example code:

```python
func3(1)
```

And some assertions:

```rust
1 == 1
```

---

## with\_defaults

```python
def with_defaults(explicit_default: [str.type] = None, hidden_default: [str.type] = None, string_default: str.type = None) -> None
```
