# Consumer OUTSIDE the cap. Even though `intersect/public_target:t`
# declares `visibility=["PUBLIC"]`, the cap clips it to
# `root//intersect/...` -- so this consumer cannot depend.
stub(
    name = "c",
    deps = ["root//intersect/public_target:t"],
)
