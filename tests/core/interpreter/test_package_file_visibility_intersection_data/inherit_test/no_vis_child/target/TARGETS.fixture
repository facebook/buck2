# PUBLIC target inside the no-visibility-arg child PACKAGE. With the
# bug, the cap would be empty and even the inside consumer would be
# rejected. With the fix, the cap stays at parent's
# `root//inherit_test/...` and inside consumers succeed.
stub(
    name = "t",
    visibility = ["PUBLIC"],
)
