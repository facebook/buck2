# @generated by autocargo from //buck2/shed/static_interner:static_interner

[package]
name = "static_interner"
version = "0.1.3"
edition.workspace = true
description = "Similar to `internment` crate, but with interface and performance tweaks"
repository.workspace = true
license.workspace = true

[dependencies]
allocative = { version = "0.3.6", path = "../../allocative/allocative", optional = true }
dupe = { workspace = true, optional = true }
equivalent.workspace = true
lock_free_hashtable.workspace = true
strong_hash = { workspace = true, optional = true }

[features]
allocative = ["dep:allocative"]
default = []
dupe = ["dep:dupe"]
strong_hash = ["dep:strong_hash"]

[lints]
workspace = true
