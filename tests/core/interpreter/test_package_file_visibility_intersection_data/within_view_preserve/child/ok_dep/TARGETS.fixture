# Allowed: the dep is under `within_view_preserve/...`, matching the
# inherited within_view. Visibility is PUBLIC -- silently clipped to
# the cap (no rejection) by intersection.
stub(
    name = "c",
    visibility = ["PUBLIC"],
    deps = ["root//within_view_preserve/dep_target:ok"],
)
