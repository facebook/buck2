[package]
name = "async_std_ext"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
futures = "0.3"

[dev-dependencies]
tokio = {version = "1.5", features = ["macros", "rt-multi-thread"]}
