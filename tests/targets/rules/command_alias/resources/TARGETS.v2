native.export_file(
    name = "exe.sh",
    mode = "reference",
)

native.command_alias(
    name = "rule",
    exe = ":exe.sh",
    resources = [
        "resource.dat",
    ],
)

native.genrule(
    name = "check",
    cmd = "$(exe :rule) | grep HELLO > $OUT",
    out = "out.txt",
)
