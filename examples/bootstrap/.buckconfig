[repositories]
root = .
prelude = prelude
toolchains = toolchains
config = config
none = none

[repository_aliases]
ovr_config = prelude
buck = none
fbcode = none
fbsource = none

[parser]
target_platform_detector_spec = target:root//...->config//platform:default_platforms
