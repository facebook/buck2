# @generated
# To regenerate, run:
# ```
# STARLARK_RUST_REGENERATE_GOLDEN_TESTS=1 cargo test -p starlark --lib
# ```

Code:
def test(a: list[str]):
    x = a + a
    y = a * 3
    z = 3 * a

No errors.

Types:
x: list[str]
y: list[str]
z: list[str]

Compiler typechecker (eval):
No errors.
