[cells]
  root = .

[buildfile]
  name = TARGETS.fixture

[buck2]
  load_as_allowlist = *.lock

[project]
  # Isolated mini-repos used only by the cross-cell / cross-package tests
  # (each selected via data_dir); ignored so the default `//...` tests skip them.
  ignore = cross_cell,cross_package
